# 对话历史记录 - v1.3 日志位置优化与发布清理 (2026-02-28)

## 核心上下文
用户反馈软件打开后仅显示控制台且无后续反应，并要求将日志文件放置在软件所在文件夹内，同时清理有问题的版本。

## 关键决策
1.  **日志路径重构**：修改了 [Launcher.java](file:///d:/TraeAIProJect/gold-price-tracker/src/main/java/com/goldpricetracker/Launcher.java)，通过 `Launcher.class.getProtectionDomain().getCodeSource().getLocation()` 动态获取可执行文件所在路径。现在，日志文件 `gold_price_tracker.log` 会自动生成在软件 EXE 同级目录下，方便用户随时查看。
2.  **启动调试增强**：在 `Launcher.main` 入口增加了启动阶段的控制台输出，以便在 `--win-console` 模式下观察程序是否成功进入引导逻辑。
3.  **环境清理**：
    -   删除了 `release/1.3/` 目录下之前打包失败或有问题的旧版本文件夹。
    -   删除了项目根目录下的旧日志文件。
4.  **重新打包方案**：延续了模块化打包方案，并确保 `jpackage` 命令包含所有必要的 JavaFX 模块及运行时权限。

## 项目进展
- 日志系统现在更加符合分发软件的标准（日志跟随软件）。
- 清理了所有冗余的错误版本，确保交付物纯净。
- 优化了启动引导代码，增加了更多的环境信息记录（Java 版本、操作系统、运行路径等）。

---
*记录人: AI 助手*
