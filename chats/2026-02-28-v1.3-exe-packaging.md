# 对话历史记录 - v1.3 EXE 免安装版本打包 (2026-02-28)

## 核心上下文
用户要求将 Java 代码打包成免安装运行的 EXE 软件，并放置在 `release/1.3/` 目录下，命名为 `gold-price-tracker-1.3.0.exe`。

## 关键决策
1.  **工具选择**：使用 JDK 21 自带的 `jpackage` 工具进行打包。
2.  **打包模式**：选择 `--type app-image`（应用程序映像）模式，这会创建一个包含最小化运行时 (JRE) 的文件夹，实现真正的“免安装运行”。
3.  **命名规范**：
    -   输出文件夹命名为 `gold-price-tracker-1.3.0.exe`。
    -   内部可执行文件命名为 `gold-price-tracker-1.3.0.exe`。
4.  **配置细节**：
    -   主 JAR：`gold-price-tracker-1.3.0.jar`
    -   主类：`com.goldpricetracker.Launcher`
    -   启用了 `--win-console` 以便于调试（后续生产版本可关闭）。

## 项目进展
- `jpackage` 打包成功。
- 最终产物位于 `release/1.3/gold-price-tracker-1.3.0.exe/`。
- 内部包含 `gold-price-tracker-1.3.0.exe` 执行文件及其依赖的运行时环境。

---
*记录人: AI 助手*
